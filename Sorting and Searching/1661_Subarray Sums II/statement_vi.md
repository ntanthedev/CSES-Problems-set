# 1661 - Subarray Sums II

* **Giới hạn thời gian:** 1.00 s
* **Giới hạn bộ nhớ:** 512 MB

Cho một mảng gồm $n$ số nguyên, nhiệm vụ của bạn là đếm số lượng đoạn con có tổng bằng $x$.

# Dữ liệu vào

Dòng đầu tiên chứa hai số nguyên $n$ và $x$: kích thước của mảng và tổng mục tiêu $x$.

Dòng tiếp theo chứa $n$ số nguyên $a\_1,a\_2,\dots,a\_n$: các phần tử của mảng.

# Kết quả

In ra một số nguyên: số lượng đoạn con cần tìm.

# Giới hạn

* $1 \le n \le 2 \cdot 10^5$
* $-10^9 \le x,a\_i \le 10^9$

# Ví dụ

Dữ liệu vào:

```cpp
5 7
2 -1 3 5 -2
```

Kết quả:

```cpp
2
```
